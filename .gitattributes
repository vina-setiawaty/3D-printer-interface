# G-code golden fixtures are compared byte-for-content by
# tests/parametric/golden-brushes.mjs; keep them as LF so a Windows
# checkout (core.autocrlf=true) doesn't rewrite them to CRLF and make an
# unrelated line-ending diff look like a library regression.
tests/parametric/golden/*.gcode text eol=lf
